<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ホームページ</title>
</head>
<body>
    <p>Hi This is home page. choose your chat room what you wanna join. or make your chat room</p>
    <a href="/create/chat">チャット作成</a>
    <% if ( datas.length > 0) { %> 
      <% datas.array.forEach(data => { %>
        <div>

          <h3><a href="/chat/<%= data.id %>  %> "><%= data.title %></a></h3> 
          
        </div>
      <% }) %>  
    <% }else{ %>
        <p>妥当する投稿が見つかりませんでした....</p>    
    <% } %> 
    </body>
</html>